# Path to fine-tuned BMD-45 YOLO weights. If this file doesn't exist,
# the backend automatically runs in DEMO MODE.
MODEL_PATH=weights/best.pt

# Confidence threshold for real YOLO inference
YOLO_CONF_THRESHOLD=0.35

# Set to "true" to force DEMO MODE even if weights are present
FORCE_DEMO_MODE=false
